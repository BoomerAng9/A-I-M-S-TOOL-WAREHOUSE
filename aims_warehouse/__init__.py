"""A.I.M.S. Tool Warehouse — the builder-resource catalog service.

A standalone, independently-deployable FastAPI service that exposes a curated,
certified-by-default catalog of the tools and resources a builder needs — model
gateways, databases, deployment targets, RAG/vector libraries, API connectors,
agent orchestration, storage, and the live health of deployed integration tools.
"""

__version__ = "0.1.0"
